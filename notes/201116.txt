201116

알림
평가 : 20시간 당 1개. 커트라인(60점)넘으면 통과. 미통과 시 2차시험
	> 내일 11/17 평가예정. 트렌드조사(?) 파워포인트로 작성.
수료기준 : 전체 출석 80%이상 수료, 모든 평가 완료

포토샵 
[시안 만들기]
파일 이름 명명 규칙 : 파일 보관시 
"banner_coffee01" (용도_파일명n) 으로 저장
ex) icon_home, icon_previous
*웹 형식 저장시 GIF는 화질이 많이 떨어지고, PNG24S는 용량이 매우 커진다.
많은 경우 JPEG나 PNG8을 이용한다.
PSD파일의 경우 포토샵 프로그램에서 쓰는 원본 소스로 용량이 크다. 
웹 형식 저장을 하게 되면 가이드, 레이어가 존재하지 않는 대신 용량이 줄어든다.
marquee툴을 이용한 영역으로 텍스트를 입력하면 텍스트 상자 크기가 고정된다.
[단축키]
레이어 복사 (ctrl+J) 모양 변경(ctrl+T) 선택해제(ctrl+D)
웹 형식 저장(ctrl+alt+shift+S) - WBMP는 표준 형식 아님(구글에서만 사용)


코딩
*head는 웹페이지가 동작하기 위한 여러가지 정보가 포함되는 영역. 
단, title만은 head에 들어간다. body는 웹페이지의 컨텐츠가 포함되는 영역
<tag __> __ : attribute
tag는 tag로만 구성되는 엘리먼트(ex)section)가 있고,
tag+attribute로 구성되는 엘리먼트가 있다. 이 경우 닫는 태그</>가 없다.
img src=이후 :
.은 웹페이지가 존재하는 현재 경로 위치 ./는 하위 경로 ../는 상위 경로
alt=이후 : 현재 컨텐츠에 대한 설명 부여
포토샵에서 설정했던 순서로 코딩(용지설정->그리드->
div "id" : 서로다른 두 개의 div을 구별하기 위해 div를 대체하는 고유 태그 부여
상속 : 상위 단계에서 헤더에 설정한 스타일, 구조 등은 하위 단계의 요소에도 적용됨
width는 상속, height는 상속 안됨.
(왜?자식 단계에서 설정된 값이 부모 단계 설정값을 넘을 수 없으므로
오히려 부모 단계 설정값이 자식 단계 값에 따라 자동 설정됨)
*이미지 파일은 부모 단계 width 속성을 상속받지 않는다.
HTML+CSS=>정적인 페이지 + javaScript=>동적인 페이지
*원칙적으로 java 코드는 /div, /body 태그 사이에 넣는다.(문서 거의 마지막)
.innertext = tag사이의 텍스트 컨텐츠를 지정
href : 외부 스타일 시트
script src : 외부 스크립트
alert('~'); : ~를 페이지 표시 전 출력
*크롬의 개발자 도구(f12)를 통해서 바뀐 형태의 웹을 시뮬레이션해 볼 수 있다.
# = id, . = class
크롬 웹페이지 검사 도구에서
개발자가 설정한 요소는 정체로, 요소가 default로 가지고 있는 설정은 이탤릭체로 표시된다.

일러스트레이터
*비트맵과 벡터의 차이 
비트맵 : 이미지를 사각형 형태의 픽셀로 저장. 일반 웹에서는 비트맵 방식으로 저장한다.
벡터 : 두 점 형태의 경로(path)를 계산
pen tool의 점 : anker point. 그만 그으려면 ctrl+lb. 
곡선을 그으려면 첫 점을 찍은 상태에서 드래그. 이 때 양쪽 점을 handle이라 한다.
일러에서는 채색사각형이 fill/stroke 이다
패스파인더 패널 안에서
divide : 패스 나누기, unite : 패스 병합(맨 위 레이어 개체로)
minus front : 앞면 오브젝트 패스 부분 삭제
selection tool/direct selection tool 차이 : dst는 선택된 앵커포인트(점)만 이동
텍스트의 경우 type tool로 만들어졌으므로 초기 상태에서는 패스가 존재하지 않는다
[단축키]
ctrl+Y : 아웃라인 보기
alt+ctrl+[] : 다음 레이어 개체 선택
shift+ctrl+[] : 선택 레이어 위치 변경
shift+ctrl+F9 : 패스파인더 - 아이콘 만드는 데 자주 사용
shift+ctrl+o : 텍스트 아웃라인 생성